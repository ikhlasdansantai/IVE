export default function AuthLayouts({ children }: { children: React.ReactNode }) {
  // return <div className="h-screen flex justify-center items-center bg-[radial-gradient(ellipse_at_top,var(--tw-gradient-stops))] from-sky-400 to-blue-800">{children}</div>;
  return <div className="h-screen flex justify-center items-center">{children}</div>;
}
